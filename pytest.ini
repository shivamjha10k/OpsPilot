[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = backend
testpaths = backend/tests
markers =
    unit: fast isolated tests
    integration: tests requiring PostgreSQL or other service infrastructure
    security: authentication, authorization, and safety-boundary tests
    e2e: complete workflow tests
addopts = -ra
